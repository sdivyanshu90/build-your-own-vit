# Minimal Prometheus scrape config for the ViT service.
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: vit-api
    metrics_path: /metrics
    static_configs:
      - targets: ["vit-api:8080"]
